# Copy this file to `.env` and adjust values as needed.
# Never commit your real `.env` with secrets.

# Application environment
ENVIRONMENT=local
LOG_LEVEL=INFO

# MLflow tracking backend
# For local dev, a file store is sufficient. Example alternatives:
# - http://mlflow:5000 (remote tracking server)
# - postgresql+psycopg2://user:pass@host:5432/dbname (via MLflow plugin)
MLFLOW_TRACKING_URI=file:./mlruns
