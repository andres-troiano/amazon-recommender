# Copy this file to `.env` and adjust values as needed.
# Never commit your real `.env` with secrets.

# Application environment
ENVIRONMENT=local
LOG_LEVEL=INFO

# MLflow tracking backend
# For local dev, a file store is sufficient. Example alternatives:
# - http://mlflow:5000 (remote tracking server)
# - postgresql+psycopg2://user:pass@host:5432/dbname (via MLflow plugin)
MLFLOW_TRACKING_URI=file:./mlruns

# ETL defaults
# Default location for the raw Electronics reviews file. Supports .json or .json.gz
RAW_REVIEWS_PATH=data/raw/reviews_electronics.json.gz
# Public dataset URL (Stanford SNAP, Electronics 5-core)
RAW_REVIEWS_URL=https://snap.stanford.edu/data/amazon/productGraph/categoryFiles/reviews_Electronics_5.json.gz
# Minimum interactions per user/item
MIN_INTERACTIONS=5
