This directory stores datasets used by the project.

- `raw/`: raw source data (e.g., Amazon Electronics reviews subset)
- `processed/`: cleaned and feature-engineered parquet tables

Do not commit large datasets to version control. Prefer storing them in cloud
object storage or download scripts.

